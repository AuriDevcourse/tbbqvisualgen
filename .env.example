# Feedback button (existing)
RESEND_API_KEY=
FEEDBACK_EMAIL=

# Team library — Neon Postgres (Vercel Marketplace integration sets this on Vercel;
# copy the pooled connection string here for local dev)
DATABASE_URL=

# Auth.js v5 — Google SSO gated to @techbbq.org
# Generate with: npx auth secret
AUTH_SECRET=
# Google OAuth client (Cloud Console). Authorized redirect URIs:
#   http://localhost:3000/api/auth/callback/google
#   https://tbbqvisualgen.vercel.app/api/auth/callback/google
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
